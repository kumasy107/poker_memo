# poker_memo

実際のポーカープレー中に、他プレイヤーのアクションを手早く記録し、後で見返す・傾向を読み解くための自宅PC+スマホ用アプリ。9maxテキサスホールデム前提。

## 技術構成
- サーバー: Node.js + Express（`server/index.js`）
- DB: SQLite / `better-sqlite3`（`server/db.js`）。`data/poker_memo.db`（gitignore対象）
- フロントエンド: ビルド不要のバニラJS/HTML/CSS（`public/`）。ハッシュルーティングのSPA
- AI分析（任意機能）: `@anthropic-ai/sdk` で `claude-opus-5` を呼び出し。`ANTHROPIC_API_KEY` 未設定でも他機能は通常通り動作する

## サーバー起動
このマシンでは `node`/`npm` がPATHに無いことがある。起動時は `.claude/skills/start-server` を参照すること。通常は `npm start` で `http://localhost:3000`。

## データモデル（座席ベース）
- 9席固定: `seat_no` 0=自分、1〜8=A〜H（着座位置基準、セッション中は固定）
- 各セッション作成時に9席が自動生成される。各席は `status`(active/away/empty)・`memo`(自由記述/クイックタグ)・`stack`(BB)・`generation`(人が交代したら+1し記録をリセット)を持つ
- ハンド開始時に「その時点で active な席」をスナップショットして参加者にする（`hand_seats`）。ハンド進行中に誰かが away/empty になった場合、以降そのハンドのアクション対象から自動的に除外される（statusは都度最新を参照、スナップショットしない）
- ディーラーボタン(BTN)は時計回りに移動。A〜Hのラベルは自分から反時計回りに固定配置される（セッション作成時に自分の物理的な着席位置を選べる）
- 金額は全てBB単位（BB=1固定、SBはBBに対する比率で入力する）。ポット・コール額・レイズのクイック額（BBの倍数／ポットの%）はサーバー側で毎回計算する（`computePotInfo`）
- オールインしたプレイヤーは、そのストリート内の巻き戻り（他家の再レイズ）でも以降のストリートでも二度とアクション対象にならない

## 画面構成
- セッション詳細画面（`#/session/:id`）が1つで「進行中のハンドがあればその場に埋め込み表示」「無ければ席管理（在席に戻す/一時離席/終了/交代、同じ粒度のボタン）」を兼ねる。別画面への遷移はない
- ハンド画面はテーブル図（自分の位置を基準に固定、ディーラーが時計回りに移動）＋現在のアクション操作の1画面構成
- ストリート内の全員のアクションが揃うと、ボタン操作なしで自動的にボード公開のカード選択に遷移する
- リバーでベットが揃うとショーダウン画面に遷移し、役の自動判定で勝者を提案する（引き分け＝チョップも検出）
- アクションログは「▽ログを見る」で折りたたみ表示（プリフロップ/フロップ/ターン/リバーのタブ切り替え）
- 参加していない席（away/empty）はテーブル上に空欄として明示表示する

## 記録・分析
- VPIP（プリフロップの自発的参加率）を集計し、プレイヤーをタップしたシートに表示する
- プリフロップで降りただけのハンドは詳細アクションを保持する必要はない（AI分析へ送るデータも簡略化する）
- 勝者決定後に「AI分析」ボタンで、自分のプレーの100点満点評価と、対戦相手の過去傾向に基づくエクスプロイトのコツ・プレイスタイルタグの自動付与を行う（ボタンを押した時だけ該当ハンドの記録が外部APIに送信される）

## 方針
- 見た目は標準的なポーカー用語（フォールド/コール/レイズ等）を使用する。過度な偽装よりも実戦での視認性・速度を優先する
